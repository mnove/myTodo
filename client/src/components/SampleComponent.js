import React from 'react'

function SampleComponent(props) {
    return (
        <div>

        <p>{props.person.name.toUpperCase()}</p>
            
        </div>
    )
}

export default SampleComponent
